query GetSpecies($page_num: Int, $page_size: Int, $updated_before: DateTime, $updated_after: DateTime) {
  species(page: $page_num, pagesize: $page_size, updatedBefore: $updated_before, updatedAfter: $updated_after) {
    id
    description
  }
}
